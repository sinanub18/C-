// + opearot overloading as frnd function
/*#include<iostream>
using namespace std;
class add
{
	int n;
	public:
		add(int i)
		{
			n = i;
		}
		void operator+(add);

};
void add::operator+(add a1)
{
	cout<<"\nSum="<<this->n + a1.n;
}

int main()
{
	add ob1(10);
	add ob2(20);
	add ob3(30);
	ob1 + ob2; // this is interpreted by compiler as ob1.operator+(ob2);
	ob3 + ob2; // this is interpreted by compiler as ob3.operator+(ob2);
	return 0;
}
*/



